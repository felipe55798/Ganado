import { CUSTOM_ELEMENTS_SCHEMA, NgModule } from '@angular/core';
import { ReproductionRoutingModule } from './reproduction-routing.module';

@NgModule({
  imports: [
    ReproductionRoutingModule
  ],
  declarations: [
    ],
  providers:[
  ],
  schemas:[CUSTOM_ELEMENTS_SCHEMA]
})
export class ReproductionModule { }
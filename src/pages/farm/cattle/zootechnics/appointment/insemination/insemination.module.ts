import { CUSTOM_ELEMENTS_SCHEMA, NgModule } from '@angular/core';
import { InseminationRoutingModule } from './insemination-routing.module';

@NgModule({
  imports: [
    InseminationRoutingModule
  ],
  declarations: [
    ],
  providers:[
  ],
  schemas:[CUSTOM_ELEMENTS_SCHEMA]
})
export class InseminationModule { }